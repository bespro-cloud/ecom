import { GROWTH_AUDIT_ACTIONS } from '@health/types';

/**
 * Re-exported so growth services import their audit vocabulary from one place,
 * as the catalogue, commerce, compliance and lifecycle modules do.
 */
export { GROWTH_AUDIT_ACTIONS };
