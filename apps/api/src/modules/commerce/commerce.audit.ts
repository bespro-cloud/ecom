import { COMMERCE_AUDIT_ACTIONS } from '@health/types';

/**
 * Re-exported so commerce services import their audit vocabulary from one
 * place, the way the catalogue module does.
 */
export { COMMERCE_AUDIT_ACTIONS };
