/**
 * Catalogue audit actions.
 *
 * Defined in `@health/types` alongside every other action so the admin log
 * viewer's filter list cannot drift from what is written. Re-exported here so
 * catalogue code imports from its own domain.
 */
export { CATALOGUE_AUDIT_ACTIONS } from '@health/types';
