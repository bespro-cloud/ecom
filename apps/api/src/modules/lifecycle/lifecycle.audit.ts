import { LIFECYCLE_AUDIT_ACTIONS } from '@health/types';

/**
 * Re-exported so lifecycle services import their audit vocabulary from one
 * place, as the catalogue, commerce and compliance modules do.
 */
export { LIFECYCLE_AUDIT_ACTIONS };
