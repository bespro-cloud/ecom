import { COMPLIANCE_AUDIT_ACTIONS } from '@health/types';

/**
 * Re-exported so compliance services import their audit vocabulary from one
 * place, the way the catalogue and commerce modules do.
 */
export { COMPLIANCE_AUDIT_ACTIONS };
